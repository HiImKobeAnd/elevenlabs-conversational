[project]
name = "elevenlabs-conversational"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hiimkobeand", email = "hiimkobeand@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "dotenv>=0.9.9",
    "elevenlabs>=2.63.0",
    "fastapi[standard]>=0.141.1",
    "pyaudio>=0.2.14",
]

[project.scripts]
elevenlabs-conversational = "elevenlabs_conversational:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
